# OS
.DS_Store

# Local Claude Code settings (machine-specific, not part of the plugin)
.claude/
.claude/settings.local.json

# Node
node_modules/
npm-debug.log*

# Packed tarballs
*.tgz

# Generated research outputs (use-case dossiers/docs are produced at runtime)
doc/
output/
logs/

# Generated design-system token extracts (regenerate via `npm run extract:tokens`).
# Design systems themselves live OUTSIDE this repo — only the bundled demo under
# design-system/ is committed; teams point at their own via the brief / $DESIGN_SYSTEM_ROOT.
design-system/**/tokens.css
design-system/tokens.css
