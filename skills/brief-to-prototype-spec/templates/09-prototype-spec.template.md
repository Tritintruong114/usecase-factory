# Prototype Spec — ws-<slug> ("<Tên hiển thị>")

<!--
Generated by usecase-factory's /usecase-factory:brief-to-prototype-spec — a one-way bridge into
the internal toc-use-cases + use-case-renderers pipeline (private repos, not part of this plugin).
Shaped to match the prototype-spec.md that toc-use-cases' design-to-renderer skill reads as its
Phase-0 locked-design input, alongside mockups.md.

NOT a Decision Pack artifact — do not route to it from 00-START-HERE.md. It exists only for teams
that also run toc-use-cases and want to graduate this workspace into design-to-renderer without
hand-transcribing Agent-Domain-Spec.md / screens-brief.md.

Verify against a real prototype/build before treating any section as fully locked — sections built
in Mode A (no live prototype) are inferred from screens-brief.md + Agent-Domain-Spec.md, not code.
-->

> **Mode:** `<A — inferred from screens-brief.md + Agent-Domain-Spec.md | B — extracted from a real to-prototype build>`
> **Generated:** `<date>` from `Agent-Domain-Spec.md` + `screens-brief.md` + `appendix/mockups.md`
> `<+ new-design/src/workspaces/<ws>/ (Mode B only)>`.
> **Handoff note:** the consuming pipeline expects `mockups.md` at the workspace ROOT — carry
> `appendix/mockups.md` alongside this file when handing the workspace off; do not duplicate it here.
> **Status:** ▶ DRAFT — not yet grilled on the receiving side.

## 1. Screen inventory + component tree

<!-- Mode A: one subsection per screen in screens-brief.md's "## Screens (v0)" — Must-show + Primary/
     Secondary action + the ASCII layout shape from appendix/mockups.md. Mark "(inferred — no live
     prototype)". Mode B: read from new-design/src/workspaces/<ws>/index.jsx + <tab>.jsx, mechanical. -->

### <S1 — Screen name>
- Layout shape: `<from appendix/mockups.md ASCII — e.g. master-detail, tab strip + card grid, wizard>`
- Must-show: `<from screens-brief.md>`
- Primary / secondary action: `<...>`
- Source: `<appendix/mockups.md (Mode A) | new-design/src/workspaces/<ws>/<tab>.jsx (Mode B)>`

## 2. State machine (per screen)

<!-- One table per screen. Every state needs either a real trigger or an explicit <<DEFER: grill>>. -->

### <S1 — Screen name>
| State | Meaning | Reached by (action) |
|---|---|---|
| empty | … | initial / no data |
| <state> | … | `<action from screens-brief.md, or <<DEFER: grill>>>` |

## 3. Data contract (FE↔BE lock)

<!-- Primarily from Agent-Domain-Spec.md §3 Core objects (entity shape) + §4 Object lifecycle (enum)
     + §12 Tool/action policy (side-effects). This is the section our upstream is strictly richer
     for than a typical prototype fixtures.js guess — don't skip it. -->

- **sessionKey-equivalent**: `<propose ONLY if a §3 core object's identifier key is session/
  conversation-shaped, else <<DEFER: grill>> — never invent>`
- **<Entity, from Agent-Domain-Spec.md §3>** schema:
  ```
  { field: type, … }   <!-- from "field agent quan tâm" column -->
  ```
  enums: `<field>` ∈ `{ …, … }`   <!-- from §4 "States hợp lệ" -->
  side-effects: `<from §12 tool/action policy>`

## 4. Interaction / flow notes

<!-- From Agent-Domain-Spec.md §2 role split, §7 eligibility, §8 decision policy, §15 learning loop —
     what mutates what, auto vs proposed, ordering constraints. -->

- …

## 5. Locked copy

<!-- Mechanical — from screens-brief.md's Copy register + per-screen Copy blocks. -->

- `<screen>`: page title = "<...>" · subtitle = "<...>" · primary action = "<...>" · empty-state = "<...>"

## Open questions for grill

<!-- Every <<DEFER: grill>> emitted above, collected here. Same marker convention design-to-renderer's
     own upstream (prototype-to-spec) uses, so a team running grill-with-docs on the receiving side
     recognizes it without translation. -->

- [ ] …
